---
# High-level pegleg site definition file
schema: pegleg/SiteDefinition/v1
metadata:
  schema: metadata/Document/v1
  layeringDefinition:
    abstract: false
    layer: site
  # NEWSITE-CHANGEME: Replace with the site name
  name:
  storagePolicy: cleartext
data:
  # Deprecated revision system, will be removed later. Do not modify.
  # The type layer this site will delpoy with. Type layer is found in the
  # aic-clcp-manifests repo.
  site_type: cruiser
  repositories:
    global:
      revision:
      url: ssh://REPO_USERNAME@codecloud.web.att.com:7999/ST_CCP/aic-clcp-manifests
    secrets:
      revision: master
      url:
    policies:
      revision: master
      url: ssh://REPO_USERNAME@codecloud.web.att.com:7999/ST_CCP/nc-network-manifests
...
